# AI 工作规范

本文件用于约束 AI 在本仓库中的工作方式：通用约定与核心原则常驻生效，专项规范按场景路由、按需加载（见三）。

---

## 一、通用约定

### 1. 语言与表达

- **语言**：回答一律使用中文；代码、命令、配置项、标识符等保持原文。

### 2. 沟通风格

- **风格**：简洁直给，先结论后展开，避免冗余铺垫。

### 3. Windows 中文编码约定

在 Windows 中文环境下，如果遇到中文乱码，优先按编码问题处理。

#### Python 相关命令优先启用 UTF-8 Mode

- `python -X utf8 script.py`
- 或先设置环境变量：`PYTHONUTF8=1`

#### 文件读写必须显式指定编码

- UTF-8 文件使用：`encoding="utf-8"`
- GBK / CP936 文件使用：`encoding="gbk"`

#### 调用 Python 子进程时，也要传递 UTF-8 设置

- 优先使用 `python -X utf8`
- 或给子进程环境变量加上 `PYTHONUTF8=1`

> **不要** 把 `chcp 65001` 或 `LANG=zh_CN.UTF-8` 当作 Python 编码修复方案。

### 4. 时间格式约定

- **格式**：所有时间非特殊说明必须使用 `YYYY-MM-dd HH:mm:ss` 格式。
- **时区**：固定为 UTC+8（Asia/Shanghai），不随客户端或系统本地时区变化。
- **保存与显示一致**：数据库存储、日志记录、接口返回、界面显示等均保持同一格式和时区，禁止做任何本地时区自适应转换。

### 5. 文件删除规则

- **禁止直接删除**：禁止使用 `rm`、`del`、`Remove-Item` 或任何脚本/命令直接删除文件与目录。
- **统一删除方式**：通过 `mv` 把待删除的内容移动到 `temp` 目录下，按 `yyyymmdd` 格式区分子目录。
- **目标路径**：`temp/<yyyymmdd>/<原相对路径>`。例如 `2026-07-27` 删除 `src/old.ts`，移动到 `temp/20260727/src/old.ts`。
- **可恢复优先**：移动而非删除，便于误删找回与回滚。

### 6. 跨层改动的执行纪律

适用于所有项目、所有技术栈。凡是改动横跨两层及以上（数据层 ↔ 代码、接口定义 ↔ 实现、前端 ↔ 后端、配置 ↔ 读取方、消息生产 ↔ 消费等）时，必须遵守：

1. **契约核对**：以某个字段/key/配置项做逻辑依据（去重、分组、条件判断、映射、路由）之前，先把它的数据链路逐层追到底——每一层的名称、别名、类型、可空性都确认接得上，不凭命名想当然。典型反例：用某字段做去重 key，但上游实际从未给这个字段赋过值（查成了别名相近的另一个字段），key 恒为空，逻辑静默失效。

2. **按破坏场景验证，不按正常场景验证**：设计验证样本时，必须覆盖让逻辑「可能出错」的输入——多条、重复值、空值/null、边界值、非法值。单条正常数据跑通不等于验证过。完成改动后主动说明用了什么样本、覆盖了哪些边界。

3. **静默失败点重点排查**：对「不报错只出错结果」的环节保持警惕——自动映射/序列化丢字段、空值进集合当 key、默认值兜底掩盖缺失、catch 后吞异常、过滤条件误伤、类型/编码/大小写隐式转换。改到这类代码时，默认假设它会静默出错，专门构造能让它暴露的输入。

4. **多点改动逐点验证**：一次改动包含多个功能点时，每个点都要有独立的验证动作，不允许只验证主改动、附带改动靠目测。

---

## 二、核心原则

### 1. 第一性原则

回归事物最本质的事实，从不可再分的基本真理出发推导结论，而非依赖类比或既有经验。

流程示意：
```
问题 → 拆解为基本事实 → 验证事实成立 → 自底向上重建 → 得出结论
```

### 2. Try Fast Fail Fast 原则

快速尝试、快速失败：以最小成本验证假设，从失败中获取反馈并迭代，避免在未经验证的方向上过度投入。

流程示意：
```
假设 → 最小实现 → 快速验证 → 失败/成功反馈 → 调整迭代
```

### 3. Michael Polanyi 的理论

人类存在"隐性知识"——我们能知道的远多于能表达的；因此不能仅依赖显式规则与文档，需保留实践、经验与直觉在判断中的位置。

### 4. Karl Popper 的理论

理论的核心在于"可证伪性"：一个观点只有能被反例推翻才有意义；故应以"猜想—反驳"的方式推进，主动寻找反例而非只寻求证实。

流程示意：
```
提出猜想 → 主动寻找反例 → 被证伪则修正/放弃 → 未被证伪则暂用
```

### 5. 最简实现原则

以满足当前已确认需求为目标，优先采用最简单、最直接的实现方式。不为未来可能的需求提前设计抽象，不引入接口、工厂、泛型框架、配置层或设计模式；仅当代码已证明存在明确需求，或同一模式在至少三个具体场景中稳定出现时才抽象。允许适度重复——重复通常比错误的抽象更易修正；优先保证代码清晰、易读、易改、易删，再考虑复用与扩展。

流程示意：
```
新需求 → 最简直接实现 → 已证明需要 或 同模式已稳定出现 ≥3 次？
                        ├─ 否：保持现状，允许适度重复
                        └─ 是：才进行抽象
```

---

## 三、规范索引（按需加载）

下列专项规范分模块存放于子文件夹，按场景命中后再读取对应文件，无需预加载全部。新增规范时：在对应模块文件夹下新增 `.md`，并在此表登记一行。

### 前端 / `frontend/`

| 场景关键词 | 触发条件 | 规范文件 |
|---|---|---|
| 前端总则（常驻） | 任何前端相关任务 | `frontend/general.md` |
| Next.js dev / HMR / 构建污染 | 涉及 Next.js 15 dev 启动、HMR 失效、`next build` 与 dev 冲突、Electron 打包 | `frontend/issue/dev-hmr-restart-issue.md` |
| IME 回车发送 / 中文输入 | 涉及输入框 Enter 发送、中文/日文 IME 候选词、`isComposing` | `frontend/issue/ime-enter-send-issue.md` |
| better-sqlite3 / 原生模块 ABI | 涉及 better-sqlite3、`.node` 原生绑定、Electron 与 Node ABI 不一致 | `frontend/issue/sqlite-electron-node-abi-issue.md` |
