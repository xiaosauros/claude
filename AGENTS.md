# AI 工作规范

约束 AI 在本仓库的工作方式：通用约定与核心原则常驻生效，专项规范按需加载（见三）。

---

## 一、通用约定

### 1. 语言与风格

- **语言**：回答一律中文；代码、命令、配置项、标识符保持原文。
- **风格**：简洁直给，先结论后展开，避免冗余铺垫。

### 2. Windows 中文编码

遇中文乱码优先按编码问题处理：

- Python 优先 UTF-8 Mode：`python -X utf8` 或环境变量 `PYTHONUTF8=1`。
- 文件读写显式指定编码：UTF-8 用 `encoding="utf-8"`，GBK/CP936 用 `encoding="gbk"`。
- 调用 Python 子进程时同样传递 UTF-8 设置。
- 不要把 `chcp 65001` 或 `LANG=zh_CN.UTF-8` 当作 Python 编码修复方案。

### 3. 时间格式

- 统一 `YYYY-MM-dd HH:mm:ss`，固定 UTC+8，不随客户端/系统时区变化。
- 存储、日志、接口返回、界面显示保持同一格式和时区，禁止本地时区自适应转换。

### 4. 文件删除

- 禁止 `rm`、`del`、`Remove-Item` 等任何直接删除方式。
- 统一用 `mv` 移动到 `temp/<yyyymmdd>/<原相对路径>`（如 `src/old.ts` → `temp/20260727/src/old.ts`），可恢复优先。

### 5. 跨层改动纪律

改动横跨两层及以上（数据层↔代码、接口定义↔实现、前端↔后端、配置↔读取方、消息生产↔消费等）时：

1. **契约核对**：以某字段/key/配置项做逻辑依据（去重、分组、条件、映射、路由）前，先把数据链路逐层追到底，确认每层的名称、别名、类型、可空性都接得上，不凭命名想当然（反例：用某字段做去重 key，上游从未赋值，key 恒空，逻辑静默失效）。
2. **按破坏场景验证**：样本必须覆盖多条、重复值、空值/null、边界值、非法值；单条正常数据跑通不等于验证过，完成后说明用了什么样本、覆盖了哪些边界。
3. **排查静默失败点**：对自动映射丢字段、空值当 key、默认值兜底、catch 吞异常、过滤误伤、隐式类型/编码/大小写转换等「不报错只出错」的环节，默认假设会静默出错，专门构造输入暴露它。
4. **多点逐点验证**：多个功能点每个都要独立验证，不允许只验证主改动、附带改动靠目测。

### 6. 会话命名

- 格式 `日期:类型:内容简述`，如 `20260917:功能:新增会话命名规范`。
- 日期 `YYYYMMdd`；类型 `功能|修复|优化|重构|文档|样式|测试`，无法归类时用更贴切的简短类型词（如 `调研`、`配置`、`回滚`）；简述一句话、不带标点结尾。

---

## 二、核心原则

1. **第一性原则**：从不可再分的基本事实推导结论，不依赖类比或既有经验。
2. **Try Fast Fail Fast**：以最小成本验证假设，快速失败、快速迭代，不在未验证方向上过度投入。
3. **隐性知识**（Polanyi）：能知道的远多于能表达的；不能仅依赖显式规则与文档，保留实践、经验与直觉在判断中的位置。
4. **可证伪性**（Popper）：以「猜想—反驳」推进，主动找反例而非只求证实；被证伪则修正/放弃，未被证伪则暂用。
5. **最简实现**：优先最简单直接的实现；不为未来需求提前抽象（接口、工厂、泛型框架、配置层、设计模式），同模式稳定出现 ≥3 次或需求已证明时才抽象；允许适度重复——重复比错误抽象更易修正；优先清晰、易读、易改、易删，再考虑复用与扩展。

---

## 三、规范索引（按需加载）

专项规范分模块存放于子文件夹，按场景命中后再读取，无需预加载。新增规范：在对应模块文件夹下新增 `.md`，并在此表登记一行。

### 前端 / `specs/frontend/`

| 场景关键词 | 触发条件 | 规范文件 |
|---|---|---|
| 前端总则（常驻） | 任何前端相关任务 | `specs/frontend/general.md` |
| Next.js dev / HMR / 构建污染 | Next.js 15 dev 启动、HMR 失效、`next build` 与 dev 冲突、Electron 打包 | `specs/frontend/issue/dev-hmr-restart-issue.md` |
| IME 回车发送 / 中文输入 | 输入框 Enter 发送、中文/日文 IME 候选词、`isComposing` | `specs/frontend/issue/ime-enter-send-issue.md` |
| 终端/TUI 内 IME 吞字、误删 | xterm/终端模拟器内中文输入、候选窗左上角、连词只上屏首字、组词退格误删终端字符、`keyCode 229` | `specs/frontend/issue/ime-tui-terminal-issue.md` |
| better-sqlite3 / 原生模块 ABI | better-sqlite3、`.node` 原生绑定、Electron 与 Node ABI 不一致 | `specs/frontend/issue/sqlite-electron-node-abi-issue.md` |

### Shell / `specs/shell/`

| 场景关键词 | 触发条件 | 规范文件 |
|---|---|---|
| PowerShell 执行命令 | PowerShell / pwsh / `.ps1` / 执行策略 / PS 5.1 相关的命令执行 | `specs/shell/powershell.md` |
| Bash 执行命令 | Bash 下执行多条命令、命令合并或并行调用 | `specs/shell/bash.md` |
| 长输出截断 | diff、日志、目录列表等输出可能超长 | `specs/shell/bash.md`（Bash）/ `specs/shell/powershell.md`（PowerShell） |
